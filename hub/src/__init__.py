"""personal-pi-home — the hub package.

Everything the always-on box does lives in here as a **job**: a small,
self-contained unit that knows how to fetch something, decide whether it's
worth saying, and hand a message to the notifier. See `jobs/base.py`.
"""
